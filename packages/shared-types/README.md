# Shared types

TypeScript types generated from the FastAPI OpenAPI schema, shared by the desktop app. Wired
up once the API has real endpoints beyond `/health` — see [`../../ROADMAP.md`](../../ROADMAP.md).
